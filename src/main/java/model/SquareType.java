package model;
/**
 * The SquareType class is an enum type class and It is used to represent the type of square i.e. the square can either be made of GRASS(pieces can be placed and moved on grass) or WATER(pieces can't be placed on water)
 */
public enum SquareType {
	GRASS, WATER;
}
